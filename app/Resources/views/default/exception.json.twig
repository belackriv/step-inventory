{% if is_granted('ROLE_DEV') %}
	{{
		{
			'error': {
				'code': status_code,
				'message': status_text,
				'exception': exception.toarray
			}
		}|json_encode|raw
	}}
{% else %}
	{{
		{
			'error': {
				'code': status_code,
				'message': status_text,
			}
		}|json_encode|raw
	}}
{% endif %}
